# This alias replaces lt with an ls command that displays the size of each item, and then sorts it by size, in a single column, with a notation to indicate the kind of file.
alias lt='ls --human-readable --size -1 -S --classify'
